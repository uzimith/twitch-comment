import {Observable} from "rxjs";
import {DOMSource} from "@cycle/dom/rxjs-typings";
import {Sources, Actions, Props} from ".";

export default function intent(dom$: DOMSource, props$: Props): Actions {
    return {};
}
