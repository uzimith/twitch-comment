import { Observable } from "rxjs";
import { div, label, input, hr, h1 } from "@cycle/dom";
import { IStates, ISinks } from "../components/main";

export default function view(states: IStates): ISinks {
}
